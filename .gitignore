node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Environment & secrets — never commit
.env
.env.*
!.env.example

# Vercel
.vercel

# Supabase CLI cache
supabase/.temp

# Editor
.vscode/
.idea/

# OS
Thumbs.db
desktop.ini

# Local debug/throwaway scripts (often contain hardcoded credentials).
# Reintroduce specific files explicitly via `git add -f scratch/<file>`
# only after removing any hardcoded tokens.
scratch/

