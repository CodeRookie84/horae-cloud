// Patch chatService.ts — replace seedDefaultChannels section (lines 602-641)
const fs = require('fs');
const path = require('path');

const filePath = path.join('c:\\Users\\admin\\horae-ops\\src\\services\\chatService.ts');
let content = fs.readFileSync(filePath, 'utf8');

// Replace from the comment block start through the closing brace of seedDefaultChannels
const oldSection = content.substring(
  content.indexOf('// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// DEFAULT CHANNEL SEEDING'),
  content.indexOf('// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// MESSAGE SEARCH')
);

const newSection = `// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// DEPT / ROLE HELPERS  (shared utilities for smart membership)\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n\r\n/** Maps a department string to the channel it should belong to */\r\nexport function getDeptChannelName(department: string): string {\r\n  const d = (department || '').toLowerCase();\r\n  if (d.includes('kitchen') || d.includes('bak') || d.includes('cook')) return 'kitchen-team';\r\n  if (d.includes('front') || d.includes('sales') || d.includes('desk') || d.includes('cashier') || d.includes('floor')) return 'floor-staff';\r\n  if (d.includes('pack') || d.includes('inventory') || d.includes('store') || d.includes('dispatch')) return 'packing-team';\r\n  if (d.includes('manage') || d.includes('hr') || d.includes('operation')) return 'managers';\r\n  return 'general';\r\n}\r\n\r\n/** Returns true for any manager/supervisor/admin-level role */\r\nexport function isManagerRole(role: string): boolean {\r\n  const r = (role || '').toLowerCase();\r\n  return r.includes('admin') || r.includes('manager') || r.includes('supervisor') || r.includes('operations') || r.includes('hr');\r\n}\r\n\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// DEFAULT CHANNEL SEEDING  (smart dept-based membership)\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n\r\ninterface BasicUser { id: string; department: string; role: string; }\r\n\r\n/**\r\n * Seed default channels on first Team Talk use.\r\n * Smart membership: each user lands in their dept channel automatically.\r\n * Managers get added to ALL channels for full visibility.\r\n *\r\n *  #general       -> everyone\r\n *  #announcements -> everyone (managers post, staff read-only)\r\n *  #kitchen-team  -> Kitchen/Baking dept + all managers\r\n *  #floor-staff   -> Front desk/Sales/Cashier + all managers\r\n *  #packing-team  -> Packing/Inventory + all managers\r\n *  #managers      -> Managers/Supervisors/Admin only\r\n */\r\nexport async function seedDefaultChannels(\r\n  tenantId: string,\r\n  creatorId: string,\r\n  allUsers: BasicUser[]\r\n): Promise<void> {\r\n  const { data: existing } = await supabase\r\n    .from('chat_channels')\r\n    .select('id')\r\n    .eq('tenant_id', tenantId)\r\n    .limit(1);\r\n\r\n  if (existing && existing.length > 0) return;\r\n\r\n  const managers = allUsers.filter(u => isManagerRole(u.role));\r\n\r\n  const deptChannels = [\r\n    {\r\n      name: 'general', type: 'outlet' as ChatChannel['type'],\r\n      description: 'Whole outlet — everyone can post',\r\n      memberIds: allUsers.map(u => u.id),\r\n    },\r\n    {\r\n      name: 'announcements', type: 'announcement' as ChatChannel['type'],\r\n      description: 'Admin announcements — managers post, staff read',\r\n      memberIds: allUsers.map(u => u.id),\r\n    },\r\n    {\r\n      name: 'kitchen-team', type: 'department' as ChatChannel['type'],\r\n      description: 'Kitchen & Baking team',\r\n      memberIds: [...new Set([...allUsers.filter(u => getDeptChannelName(u.department) === 'kitchen-team').map(u => u.id), ...managers.map(u => u.id)])],\r\n    },\r\n    {\r\n      name: 'floor-staff', type: 'department' as ChatChannel['type'],\r\n      description: 'Front desk, sales & cashier team',\r\n      memberIds: [...new Set([...allUsers.filter(u => getDeptChannelName(u.department) === 'floor-staff').map(u => u.id), ...managers.map(u => u.id)])],\r\n    },\r\n    {\r\n      name: 'packing-team', type: 'department' as ChatChannel['type'],\r\n      description: 'Packing, dispatch & inventory team',\r\n      memberIds: [...new Set([...allUsers.filter(u => getDeptChannelName(u.department) === 'packing-team').map(u => u.id), ...managers.map(u => u.id)])],\r\n    },\r\n    {\r\n      name: 'managers', type: 'department' as ChatChannel['type'],\r\n      description: 'Managers, supervisors & admin only',\r\n      memberIds: managers.map(u => u.id),\r\n    },\r\n  ];\r\n\r\n  for (const def of deptChannels) {\r\n    if (def.memberIds.length === 0) continue;\r\n    const channel = await createChannel(tenantId, def.name, def.type, creatorId, def.description);\r\n    if (channel) {\r\n      await addMembers(channel.id, def.memberIds);\r\n      await sendSystemMessage(channel.id, tenantId, 'Welcome to #' + def.name + '! Team Talk is ready.');\r\n    }\r\n  }\r\n}\r\n\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// AUTO-MEMBERSHIP ON STAFF ONBOARDING\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n\r\n/**\r\n * Call this right after saving a new staff member to Supabase.\r\n * Auto-adds them to the correct channels from their dept + role.\r\n * ZERO manual group management needed.\r\n *\r\n * In store.ts, after supabase.from('users').insert([newUser]):\r\n *   await chatService.autoAddUserToChannels(tenantId, newUser.id, newUser.role, newUser.department);\r\n */\r\nexport async function autoAddUserToChannels(\r\n  tenantId: string,\r\n  userId: string,\r\n  role: string,\r\n  department: string\r\n): Promise<void> {\r\n  const channels = await getAllChannels(tenantId);\r\n  if (!channels || channels.length === 0) return;\r\n\r\n  const userDeptChannel = getDeptChannelName(department);\r\n  const userIsManager = isManagerRole(role);\r\n\r\n  for (const ch of channels) {\r\n    if (ch.name === 'general' || ch.type === 'announcement') {\r\n      await addMembers(ch.id, [userId]);\r\n      continue;\r\n    }\r\n    if (userIsManager) {\r\n      await addMembers(ch.id, [userId]);\r\n      continue;\r\n    }\r\n    if (ch.name === userDeptChannel) {\r\n      await addMembers(ch.id, [userId]);\r\n    }\r\n  }\r\n}\r\n\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n// ESCALATION\r\n// \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\r\n\r\n/**\r\n * Escalate a message to a different channel.\r\n * Posts a system message in the target channel preserving full context.\r\n * Manager-only action — always visible to the whole target channel.\r\n */\r\nexport async function escalateMessage(params: {\r\n  originalMessage: TeamTalkMessage;\r\n  fromChannelName: string;\r\n  toChannelId: string;\r\n  tenantId: string;\r\n  escalatedByName: string;\r\n  note?: string;\r\n}): Promise<void> {\r\n  const { originalMessage, fromChannelName, toChannelId, tenantId, escalatedByName, note } = params;\r\n  const preview = originalMessage.messageType === 'voice'\r\n    ? (originalMessage.voiceTranscript ? '\"' + originalMessage.voiceTranscript.slice(0, 120) + '\"' : 'Voice message')\r\n    : '\"' + (originalMessage.content || '').slice(0, 150) + ((originalMessage.content || '').length > 150 ? '...' : '') + '\"';\r\n\r\n  const body = [\r\n    'Escalated by ' + escalatedByName + ' from #' + fromChannelName,\r\n    'From: ' + originalMessage.senderName + (originalMessage.senderRole ? ' (' + originalMessage.senderRole + ')' : ''),\r\n    preview,\r\n    ...(note?.trim() ? ['Note: ' + note.trim()] : []),\r\n  ].join('\\n');\r\n\r\n  await sendSystemMessage(toChannelId, tenantId, 'ESCALATION: ' + body);\r\n}\r\n\r\n`;

content = content.replace(oldSection, newSection);
fs.writeFileSync(filePath, content, 'utf8');
console.log('Done. Lines replaced successfully.');
